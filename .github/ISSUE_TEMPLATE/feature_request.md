---
name: Feature request
about: Propose a model, a job alias, or a change to the stack
labels: enhancement
---

**What you want to do that you cannot do today**

**If this is a new job alias**
- Alias name and the job it does, in one line:
- Model behind it, and why that one rather than a smaller one:
- `litellm_params` you propose (`temperature`, `max_tokens`, `timeout`):
- Where it belongs in `router_settings.fallbacks`:
- Rough performance on CPU:

**If this is something else**
What it changes, and what it costs someone who does not want it.

**Alternatives you considered**
